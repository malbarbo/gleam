[package]
name = "builtins-int-parse"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
lexical-core = { version = "1.0.6", default-features = false, features = ["parse-integers"] }

[profile.release]
lto = true
codegen-units = 1

[workspace]
